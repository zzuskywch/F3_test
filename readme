假设遗传漂变是中性的，没有回复突变，也没有重复突变，并且 A、B、C 具有简单的系统发育，没有混合事件，那么在以下确定下，
F3( C; A , B ) =E[ ( c - a ) ( c - b ) ] ≥ 0 
1.没有确定，例如在序列数据中，
2.在一个外群中的确定，它与 R 的分离比 A、B、C 更远，
3.通过在 {A , B , C}的单个个体中找到杂合子来确定，其中我们还假设 R 的群体处于突变漂移平衡，因此多态性衍生等位基因的概率为群体频率 r ∝ 1/ r 埃文斯 (1963)。
证明。前两种情况很清楚，因为在以R为根的树的边缘上的漂移是正交的。这是主要文章中详细讨论的情况。我们确定杂合子的情况更复杂，我们的讨论涉及一些实质性的代数，这是我们用 Maple 进行的。

#命令行
qp3Pop -p parfile > OUTfile

parfile 中每个参数的描述：
genotypename：输入的基因型文件（本征格式）
snpname：输入snp文件（本征格式）
indivname：输入indiv文件（特征层格式）
popfilename: list_qp3test（每行包含3个种群<Source1 (A)> <Source2 (B)> < Target (C)>

##
